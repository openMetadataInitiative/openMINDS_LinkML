id: https://openminds.ebrains.eu/schemas/latest/enums/ProductAccessibility?format=linkml
name: openMINDS-enums-ProductAccessibility
title: OpenMINDS enum for ProductAccessibility
description: OpenMINDS enum for ProductAccessibility
license: https://spdx.org/licenses/MIT.html
prefixes:
  linkml: https://w3id.org/linkml/
  schema: http://schema.org/
  omi: https://openminds.ebrains.eu
default_prefix: omi
enums:
  ProductAccessibility:
    enum_uri: https://openminds.ebrains.eu/controlledTerms/ProductAccessibility
    title: Product accessibility
    permissible_values:
      controlled access:
        description: With 'controlled access' selected, data and metadata are both
          released and available free of charge, but users must logged in and authenticated
          themselves to access the data.
      retracted:
        description: With 'retracted' selected, metadata are released, but data are
          retracted.
      free access:
        description: With 'free access' selected, data and metadata are both released
          and become immediately available without any access restrictions.
      paid access:
        description: With 'paid access' selected, data and metadata are both released,
          but users paid to gain access to the data (e.g., a one-time fee).
      under embargo:
        description: With 'under embargo' selected, metadata are released, but data
          remain unavailable for the public until the embargo is lifted.
      restricted access:
        description: With 'restricted access' selected, metadata are released, but
          data remain on an access restricted server.
