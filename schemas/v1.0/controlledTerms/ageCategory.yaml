AgeCategory:
  class_uri: https://openminds.ebrains.eu/controlledTerms/AgeCategory
  description: Structured information on the life cycle (semantic term) of a specific
    age group.
  slot_usage:
    definition:
      range: string
    description:
      range: string
    name:
      range: string
      required: true
    ontologyIdentifier:
      range: string
  slots:
  - id
  - definition
  - description
  - name
  - ontologyIdentifier
  title: Age category
