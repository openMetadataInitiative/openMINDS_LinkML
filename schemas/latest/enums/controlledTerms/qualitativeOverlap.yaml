id: https://openminds.om-i.org/schemas/latest/enums/QualitativeOverlap?format=linkml
name: openMINDS-enums-QualitativeOverlap
title: OpenMINDS enum for QualitativeOverlap
description: OpenMINDS enum for QualitativeOverlap
license: https://spdx.org/licenses/MIT.html
prefixes:
  linkml: https://w3id.org/linkml/
  schema: http://schema.org/
  omi: https://openminds.om-i.org
default_prefix: omi
enums:
  QualitativeOverlap:
    enum_uri: https://openminds.om-i.org/types/QualitativeOverlap
    title: Qualitative overlap
    permissible_values:
      is superset of:
        description: A set/object (A) is a superset of another set/object (B) if (A)
          and (B) are not equal, but all elements/incidents of (B) are also elements/incidents
          of (A).
      partially overlaps with:
        description: Two sets/objects (A and B) partially overlap when some elements/incidents
          are part of both original objects (A and B).
      is identical to:
        description: A set/object (A) is identical to another set/object (B) if they
          look exactly the same.
      is homologous to:
        description: A set/object (A) has the same relative position, function, or
          structure as a set/object (B).
      is subset of:
        description: A set/object (A) is a subset of another set/object (B) if (A)
          and (B) are not equal, but all elements/incidents of (A) are also elements/incidents
          of (B).
